#include <stdio.h>

 int GCD(){
   
   int a, b, temp;

   printf("\nEnter two positive integers:");
   scanf("%d %d ", &a, &b);

  while (a%b !=0){
    temp = a%b;
    a = b;
    b = temp;
    }

   
   printf("\nThe GCD is %d", b);
   return 0;
 }
